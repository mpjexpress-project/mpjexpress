#!/bin/sh
#set -x

if [ $# -ne 1 ]; then
   echo "Usage: mpjhalt <machines_file>";
   exit 127
fi 

lines=`cat $1`

for i in `echo $lines`; do 
  host=`echo $i`    
  ssh $host "cd $MPJ_HOME/bin;./mpjdaemon_linux_x86 stop;"
  #ssh $host "cd $MPJ_HOME/bin;./mpjdaemon_linux_ppc_64 stop;" 
  #ssh $host "cd $MPJ_HOME/bin;./mpjdaemon_solaris_sparc_64 stop;" # solaris_sparc_64
done
